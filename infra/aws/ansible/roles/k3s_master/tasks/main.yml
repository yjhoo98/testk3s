---
- name: Install K3s master with external PostgreSQL datastore
  shell: |
    curl -sfL https://get.k3s.io | \
      K3S_DATASTORE_ENDPOINT="postgres://{{ db_username }}:{{ vault_db_password }}@{{ db_endpoint }}:{{ db_port }}/{{ db_name }}" \
      K3S_TOKEN="{{ vault_k3s_shared_token }}" \
      sh -s - server \
      --tls-san {{ nlb_dns }}
  args:
    creates: /usr/local/bin/k3s

- name: Wait for K3s kubeconfig to be ready
  wait_for:
    path: /etc/rancher/k3s/k3s.yaml
    timeout: 120

- name: Ensure K3s service is enabled and running
  systemd:
    name: k3s
    state: started
    enabled: yes
